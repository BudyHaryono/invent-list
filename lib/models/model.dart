class LoginData {
  String emailAddress;
  String password;

  LoginData({this.emailAddress, this.password});
}

class ItemList {
  String name;
  int quantity;
  String position;

  ItemList({this.name, this.quantity, this.position});
}